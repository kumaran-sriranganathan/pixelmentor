# PixelMentor Backend Dependencies
# Last updated: June 2026

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.129.1
uvicorn[standard]==0.34.2
gunicorn==23.0.0
python-multipart==0.0.20

# ── Settings + validation ─────────────────────────────────────────────────────
pydantic-settings==2.9.1

# ── AI ───────────────────────────────────────────────────────────────────────
# openai — pin to >= so Railway always gets the latest patch for the 1.x line
openai>=1.75.0

# ── Agentic AI ────────────────────────────────────────────────────────────────
# langgraph 1.2.2 — major version jump from 0.2.x; fixes the
# LangChainPendingDeprecationWarning visible in Railway startup logs.
# The StateGraph / ainvoke API used in photo_coach.py is unchanged.
langgraph==1.2.2

# ── Auth ─────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
httpx==0.28.1

# ── Supabase ──────────────────────────────────────────────────────────────────
# 2.30.1 — fixes the text_search() config= kwarg that was removed in 2.x
# and caused the photo analysis lesson recommendation crash
supabase==2.30.1

# ── Monitoring ────────────────────────────────────────────────────────────────
sentry-sdk[fastapi]==2.54.0

# ── Storage (Cloudflare R2 via S3-compatible API) ────────────────────────────
boto3==1.35.0

# ── Azure (kept for backwards compatibility — remove when Azure fully retired) ─
azure-core==1.32.0
azure-search-documents==11.4.0
azure-identity==1.19.0
azure-cosmos==4.9.0
azure-storage-blob==12.23.1
azure-monitor-opentelemetry==1.6.4
